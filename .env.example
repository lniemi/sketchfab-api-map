# Environment Variables Configuration
# Copy this file to .env and fill in your actual API tokens

# === REQUIRED ===
# Mapbox API token - get from https://account.mapbox.com/access-tokens/
# You need to create a free Mapbox account and generate an access token
VITE_MAPBOX_ACCESS_TOKEN=example_token_here

# === OPTIONAL ===
# Sketchfab API token - get from https://sketchfab.com/settings/password
# Only needed if you want to download private models from Sketchfab
# Leave empty to use public models only
VITE_SKETCHFAB_API_TOKEN=your_token_here

# Note: Environment variables in Vite must be prefixed with VITE_ to be accessible in the browser
